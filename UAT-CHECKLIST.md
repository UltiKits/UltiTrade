# UltiTrade — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the
steps to exercise it and the observable truth that proves it works. It is an internal reference
for real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`. **Nearly every meaningful row in this repository needs a
> SECOND online player** as the trade partner — a trade is inherently two-sided. Every row below
> states this explicitly in its own Preconditions rather than leaving it implied, because the
> executing agent has to arrange the second account before the row can even start.

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only,
  as `.neg-<slug>` — a negative case still tests the same feature, so the base ID is unchanged.
- **Layer**, copied verbatim from Laojun's own `ultitools-real-client-uat` skill so no
  translation step exists at dispatch time: `protocol`, `java-client`, `os-input`, `pixel`,
  `server`, `human`.
- **Human-authenticated-session rows (D-27b):** a row whose Steps can only be exercised through
  the maintainer's own authenticated UltiCloud panel session carries the fixed Preconditions
  phrase `maintainer-authenticated UltiCloud panel session (personal credentials)` and Layer
  `human`. This repository has no such row — it has no panel-capability surface of its own — so
  none is currently affected; the convention is stated here for template consistency.
- **Expected** must name an observable truth — an exact chat line, a log line, a database row,
  an inventory slot — and never the words "it works".
- **Covers** back-references a Phase 9 GUI-excluded class name; left blank when no such class
  applies. `TradeGUI` and `TradeConfirmPage` are this module's two entries in that register
  (`.planning/phases/09-module-ecosystem-readiness-and-test-coverage/gui-exclusions/
  UltiTrade.md`), so every pixel-layer row exercising either GUI names it in this column.
- A row whose Preconditions name a prior CHECKLIST row must appear after that row in file order —
  asserted mechanically: for every row, every checklist ID cited in its Preconditions cell must
  have a strictly smaller line number in this file than the row citing it (sweep class 8, D-27a).
  **A dependency on a row from a DIFFERENT, later-appearing section is described in prose (the
  command to run), never by citing that row's ID** — this avoids a forward reference while still
  telling the executor exactly how to reach the state.
- **Config-per-file rule (D-06):** one checklist row per `@ConfigEntity`-annotated class, never
  one row per key. This module ships exactly one `@ConfigEntity` (`TradeConfig`,
  `config/trade.yml`), so exactly one config row exists below (ID suffixed `-yml`,
  `ultitrade.config.trade-yml`). This module ships no Maven-filtered (build-time) config file, so
  the build-time-property clause of this rule does not apply to any row below.
- **This module's actual language behaviour (read `FEATURES.md`'s own Conventions note in full
  before any row below):** `language: en` has no observable effect anywhere in this module except
  six replies — every other player-facing string is a hardcoded Chinese literal or a Chinese-only
  config-default message. The six are the replies of `/trade toggle`, `/trade block` and
  `/trade unblock`, which come from the language catalogue as of `UltiKits/UltiTrade#17`; the six
  `ultitrade.settings.*` rows that assert one of them carry a `language: en` precondition and quote it
  verbatim from `lang/en.yml`. No other row carries a `language: en` precondition for this module's
  own strings (the two further exceptions, `ultitrade.lifecycle.reload` and
  `ultitrade.lifecycle.reload-money-trade`, set it only for two framework-owned console lines the
  framework itself localises); every other module Expected quotation is given as an English gloss of
  the actual (Chinese) text the handler emits, with the literal characters left to the source line
  cited in `FEATURES.md`, per this document's English-only rule. Filed as `UltiKits/UltiTrade#16`.
- **Item-safety is this module's own hazard class (T-10-21):** every negative row that could lose
  or duplicate an item states the EXACT item state on both sides afterward, not merely "the trade
  is cancelled".

## Trade Requests

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.request.send | Sender and a SECOND online player (the target) both hold `ultitrade.use`; neither has trading toggled off (shipped default: enabled); neither is on the other's blocklist; neither is already trading; `max-distance: 50` (shipped default) and both players within that distance in the same world; no pending request already exists between them in either direction | Run `/trade <target>` | Chat line to the sender reads (English gloss of the actual Chinese) "Trade request sent to {target}!" (green); the target receives EITHER a clickable chat message reading "{sender} wants to trade with you! [Accept] [Deny]" (if `enable-clickable-buttons: true`, shipped default) OR, if that key is false, a plain line reading "{sender} wants to trade with you! Use /trade accept to accept"; a countdown BossBar titled "{sender} wants to trade with you (30s remaining)" appears to the target (since `enable-bossbar: true`, shipped default), counting down from `request-timeout` (30, shipped default) | server | |
| ultitrade.request.send.neg-already-pending | Same as `ultitrade.request.send` above, with a request from the sender to the SAME target already pending (run `ultitrade.request.send` above first, without the target responding) | Run `/trade <target>` again, to the same target | Chat line reads (gloss) "You have already sent a trade request to this player!" (red) — no second BossBar or notification reaches the target | server | |
| ultitrade.request.send.neg-blocked-by-target | Sender and target both online, both trade-enabled; the TARGET has blocked the sender (as the target, run `/trade block <sender>` first) | As the sender, run `/trade <target>` | Chat line to the sender reads (gloss) "The other party has blocked you!" (translated from `messages.player-blocked`'s shipped Chinese default); no BossBar or notification reaches the target | server | |
| ultitrade.request.send.neg-self | Sender online, trade-enabled | Run `/trade <sender's own name>` | Chat line reads (gloss) "You cannot trade with yourself!" (red); no request is created | server | |
| ultitrade.request.send.neg-target-disabled | Sender online, trade-enabled; the TARGET has toggled trading off (as the target, run `/trade toggle` first, confirming the disabled state via the target's own `/trade` bare-command status line) | As the sender, run `/trade <target>` | Chat line to the sender reads (gloss) "The other party has disabled trading!" (translated from `messages.trade-disabled`'s shipped Chinese default); no BossBar or notification reaches the target | server | |
| ultitrade.request.send.neg-target-offline | Sender online, `<name>` names a player who is NOT currently connected (may have played before, or never) | Run `/trade <name>` | Chat line reads (gloss) "Player {name} is not online!" (red); no request is created | server | |
| ultitrade.request.send.neg-target-trading | Sender online, trade-enabled, NOT already trading; the target is already in an active trade session with a THIRD online player — arrange this directly: have the third player run `/trade <target>`, then have the target run `/trade accept`, confirming both players now see an open `TradeGUI` before proceeding | As the sender, run `/trade <target>` (the busy one) | Chat line reads (gloss) "{target} is currently trading!" (red); no request reaches the busy target | server | |
| ultitrade.request.shift-click | Same preconditions as `ultitrade.request.send` above; `enable-shift-click: true` (shipped default) | Sneak (shift) and right-click the target player's entity model with an empty or item-holding main hand | Identical outcome to `ultitrade.request.send` (same chat line, BossBar, and target notification) — this is a second entry point to the same `TradeService#sendRequest` method | os-input | |
| ultitrade.request.shift-click.neg-disabled | Same as `ultitrade.request.shift-click` above, except `enable-shift-click: false` | Sneak and right-click the target player's entity model | Nothing happens from this module's perspective — no request, no message, no BossBar; the interact event passes through unmodified (`event.setCancelled(true)` is never reached) | os-input | |
| ultitrade.request.accept | The RECEIVING player has a pending trade request from a SECOND, still-online player (send it via `ultitrade.request.send` above) | As the receiver, run `/trade accept` | Both players' `TradeGUI` windows open simultaneously (see `ultitrade.session.start` below); the receiving player's own BossBar (if shown) disappears | pixel | TradeGUI |
| ultitrade.request.accept.neg-expired | A trade request was sent (`ultitrade.request.send` above); the receiver waits at least 40 seconds (past the 30-second `request-timeout` shipped default, plus a safety margin covering the 10-second cleanup tick) WITHOUT accepting or denying, confirming the receiver's BossBar (if shown) has already disappeared on its own | As the receiver, run `/trade accept` after that wait | Chat line reads (gloss) "No pending trade request!" (red) — the scheduled cleanup task already removed the expired entry; no GUI opens | server | |
| ultitrade.request.accept.neg-none-pending | The receiver has no pending trade request from anyone | Run `/trade accept` | Chat line reads (gloss) "No pending trade request!" (red) | server | |
| ultitrade.request.accept.neg-sender-offline | A trade request is pending (`ultitrade.request.send` above), then the ORIGINAL SENDER disconnects before the receiver accepts | As the receiver, run `/trade accept` | Chat line reads (gloss) "The other party is offline!" (red); no GUI opens; the pending-request entry is consumed (a second `/trade accept` immediately after reads "No pending trade request!" instead) | server | |
| ultitrade.request.deny | The RECEIVING player has a pending trade request from a SECOND, still-online player (send it via `ultitrade.request.send` above) | As the receiver, run `/trade deny` | Chat line to the receiver reads (gloss) "Trade request denied!" (yellow); the original sender receives (gloss) "{receiver} denied your trading request!" (red) plus a refusal sound; the receiver's BossBar (if shown) disappears | server | |
| ultitrade.request.deny.neg-none-pending | The receiver has no pending trade request from anyone | Run `/trade deny` | Chat line reads (gloss) "No pending trade request!" (red) | server | |
| ultitrade.request.timeout-cleanup | A trade request is pending (`ultitrade.request.send` above); the receiver does NOT accept or deny it | Wait `request-timeout` seconds (30, shipped default) plus a small margin, without either player taking action | Within the next 10-second cleanup tick after the timeout elapses, the receiver's BossBar (if shown) disappears and the receiver gets (gloss) "Trade request has timed out!" (red); a subsequent `/trade accept` from the receiver now reads "No pending trade request!" (see `.neg-none-pending` above) | server | |

## Trading Preferences

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.settings.block | Sender online; a SECOND online player (the target) not already on the sender's blocklist, and not the sender themself; `language: en` in `plugins/UltiTools/config.yml` (it governs only the one reply this row quotes verbatim, `UltiKits/UltiTrade#17`); the module's on-disk `plugins/UltiTools/pluginConfig/UltiTrade/lang/en.yml` holds the entry named in Expected with exactly the text quoted there — the framework keeps an on-disk copy that differs from this build's, so if it differs, delete that file before starting the server so it is extracted afresh, and if it still differs record the row `blocked` | Run `/trade block <target>` | The first chat line reads exactly `Added <target> to your trade blacklist!` (green) — `lang/en.yml`'s `block_success` entry with the target's name in place of `{PLAYER}` (before `UltiKits/UltiTrade#17` it was a Chinese sentence under either `language`) — followed by (gloss) "This player will not be able to send you trade requests." (gray, still a Chinese literal under either `language`, `UltiKits/UltiTrade#16`); confirmed by the target then attempting `/trade <sender>` and receiving `ultitrade.request.send.neg-blocked-by-target`'s refusal | server | |
| ultitrade.settings.block.neg-already-blocked | Sender online; the target still online and already on the sender's blocklist (run `ultitrade.settings.block` above first); `language: en` in `plugins/UltiTools/config.yml` (it governs only the one reply this row quotes verbatim, `UltiKits/UltiTrade#17`); the module's on-disk `plugins/UltiTools/pluginConfig/UltiTrade/lang/en.yml` holds the entry named in Expected with exactly the text quoted there — the framework keeps an on-disk copy that differs from this build's, so if it differs, delete that file before starting the server so it is extracted afresh, and if it still differs record the row `blocked` | Run `/trade block <target>` again | The only chat line reads exactly `<target> is already in your blacklist!` (red) — `lang/en.yml`'s `already_blocked` entry with the target's name in place of `{PLAYER}`; the blocklist is unchanged (the target still receives `ultitrade.request.send.neg-blocked-by-target`'s refusal) | server | |
| ultitrade.settings.block.neg-offline | Sender online, `<name>` not currently connected | Run `/trade block <name>` | Chat line reads (gloss) "Player {name} is not online! Cannot add to blacklist." (red) | server | |
| ultitrade.settings.block.neg-self | Sender online | Run `/trade block <sender's own name>` | Chat line reads (gloss) "Cannot add yourself to the blacklist!" (red) | server | |
| ultitrade.settings.help | Sender online, holds `ultitrade.use` | Run `/trade` with no arguments (or `/trade help`) | Chat starts with (gloss) "=== UltiTrade Help ===" (gold), lists all 7 sub-commands (accept/deny/cancel/toggle/block/unblock/the bare request format) with a one-line description each, then ends with (gloss) "Trade status: " followed by "Enabled" (green) or "Disabled" (red) reflecting the sender's own current `TradeLogService#isTradeEnabled` state | server | |
| ultitrade.settings.toggle | Sender online, currently trade-ENABLED (the shipped default for a player never seen before); `language: en` in `plugins/UltiTools/config.yml` (it governs only the one reply this row quotes verbatim, `UltiKits/UltiTrade#17`); the module's on-disk `plugins/UltiTools/pluginConfig/UltiTrade/lang/en.yml` holds the entry named in Expected with exactly the text quoted there — the framework keeps an on-disk copy that differs from this build's, so if it differs, delete that file before starting the server so it is extracted afresh, and if it still differs record the row `blocked` | Run `/trade toggle` | The only chat line reads exactly `Trade disabled!` (yellow) — `lang/en.yml`'s `trade_toggle_off` entry (before `UltiKits/UltiTrade#17` it was a longer Chinese sentence under either `language`); confirmed by `ultitrade.settings.help` above (bare `/trade`) now showing "Disabled" | server | |
| ultitrade.settings.toggle.neg-reenable | Sender currently trade-DISABLED (run `ultitrade.settings.toggle` above first); `language: en` in `plugins/UltiTools/config.yml` (it governs only the one reply this row quotes verbatim, `UltiKits/UltiTrade#17`); the module's on-disk `plugins/UltiTools/pluginConfig/UltiTrade/lang/en.yml` holds the entry named in Expected with exactly the text quoted there — the framework keeps an on-disk copy that differs from this build's, so if it differs, delete that file before starting the server so it is extracted afresh, and if it still differs record the row `blocked` | Run `/trade toggle` again | The only chat line reads exactly `Trade enabled!` (green) — `lang/en.yml`'s `trade_toggle_on` entry; confirmed by `ultitrade.settings.help` above now showing "Enabled" again | server | |
| ultitrade.settings.unblock | Sender online; the target online and currently on the sender's blocklist (run `ultitrade.settings.block` above first); `language: en` in `plugins/UltiTools/config.yml` (it governs only the one reply this row quotes verbatim, `UltiKits/UltiTrade#17`); the module's on-disk `plugins/UltiTools/pluginConfig/UltiTrade/lang/en.yml` holds the entry named in Expected with exactly the text quoted there — the framework keeps an on-disk copy that differs from this build's, so if it differs, delete that file before starting the server so it is extracted afresh, and if it still differs record the row `blocked` | Run `/trade unblock <target>` | The only chat line reads exactly `Removed <target> from your trade blacklist!` (green) — `lang/en.yml`'s `unblock_success` entry with the target's name in place of `{PLAYER}`; confirmed by the target then successfully sending a trade request (`ultitrade.request.send` above), no longer refused | server | |
| ultitrade.settings.unblock.neg-not-blocked | Sender online; `<name>` is a SECOND player who is online (an offline name is refused as offline before the blocklist is consulted) and NOT currently on the sender's blocklist; `language: en` in `plugins/UltiTools/config.yml` (it governs only the one reply this row quotes verbatim, `UltiKits/UltiTrade#17`); the module's on-disk `plugins/UltiTools/pluginConfig/UltiTrade/lang/en.yml` holds the entry named in Expected with exactly the text quoted there — the framework keeps an on-disk copy that differs from this build's, so if it differs, delete that file before starting the server so it is extracted afresh, and if it still differs record the row `blocked` | Run `/trade unblock <name>` | The only chat line reads exactly `<name> is not in your blacklist!` (red) — `lang/en.yml`'s `not_blocked` entry with the player's name in place of `{PLAYER}` | server | |
| ultitrade.settings.unblock.neg-offline | Sender online, `<name>` not currently connected | Run `/trade unblock <name>` | Chat line reads (gloss) "Player {name} is not online! Cannot remove from blacklist." (red) | server | |

## Trade Session

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.session.start | A trade request has been accepted or auto-started (run `ultitrade.request.accept` above) | (no additional step — already exercised by `ultitrade.request.accept`) | Both players see a 6-row `TradeGUI` inventory: left glass-pane placeholders (lime, "Your Items") in the 16 `YOUR_SLOTS`, right glass-pane placeholders (cyan, "Their Items") in the 16 `THEIR_SLOTS`, black separator panes, a green "Confirm Trade" button, a red "Cancel Trade" barrier button, red/lime wool "not confirmed" status indicators on both sides, and (since `enable-money-trade`/`enable-exp-trade` default true, with no Vault installed in a Vault-less test environment) a red "Money trading not enabled" barrier in place of the money display if Vault is genuinely absent | pixel | TradeGUI |
| ultitrade.session.item-place | Both players in an active `TradeGUI` (`ultitrade.session.start` above); the item to offer is sitting in one of the acting player's own 36 inventory slots — the ones the open trade window shows below its own six rows — and the acting player's cursor is empty | With the trade window open, plain left-click that own inventory slot to lift the whole stack onto the cursor, then plain left-click one of the acting player's own 16 "Your Items" slots (a lime glass-pane placeholder). The first click is the gesture that loads the cursor and it is the only one that does: from an own inventory slot a shift-click, a double-click collect and a number-key swap are all refused, and a stack carried before the window opened is no longer on the cursor at all, because the server returns or drops it when the trade window replaces the previous screen (`UltiKits/UltiTrade#39`) | The first click is NOT refused: the stack leaves the own inventory slot and sits on the cursor. On the second click the item moves from the cursor into that slot in the acting player's OWN GUI (glass pane replaced by the real item); the item ALSO appears (with an added detail-lore block: enchantments/durability/flags/unbreakable/custom-model, where applicable) in the corresponding "Their Items" slot of the OTHER player's GUI; both sides' confirmation status resets to "not confirmed" if either was previously confirmed; a note-block-pling sound plays to the placing player | pixel | TradeGUI |
| ultitrade.session.item-place.neg-stored-glass-pane | Both players in an active `TradeGUI` (`ultitrade.session.start` above); the acting player has placed a stack of a stained glass pane of their own — the same material family the empty-slot placeholder uses, for example 7 lime stained glass panes — into one of their own item slots via `ultitrade.session.item-place` above; the acting player records how many panes of that exact material they are carrying immediately before the click, since a return merges into any partial stack they still hold rather than reappearing as a separate 7-stack; the acting player then lifts a clearly different item (for example one diamond) from another of their own inventory slots onto the cursor with a plain left-click, exactly the gesture `ultitrade.session.item-place` above describes, and their regular inventory has at least one empty slot | Click that same slot again while holding the diamond; then click the slot once more with an empty cursor | After the first click the pane is NOT destroyed: the slot shows the diamond in the acting player's own GUI and, with the detail-lore block appended (enchantments/durability/flags/unbreakable/custom-model, where applicable, exactly as `ultitrade.session.item-place` describes), in the other player's "Their Items" side; the cursor is empty; and the acting player's count of that pane material is 7 higher than the number they recorded before the click (had the inventory been full, the 7 panes would instead be lying on the ground at their feet). After the second click the diamond returns to the acting player's inventory and the slot reverts to its glass-pane placeholder in both GUIs — so an offered pane is never stuck in the slot and never disappears. Both sides' confirmation resets on each click | pixel | TradeGUI |
| ultitrade.session.item-remove | An item was placed by the acting player in one of their own slots (`ultitrade.session.item-place` above); the acting player's regular inventory has at least one empty slot | Click that same slot again (now showing the real item, not a glass pane) with an EMPTY cursor — the same click while holding an item swaps instead of removing, which is `ultitrade.session.item-place.neg-stored-glass-pane` above | The item returns to the acting player's own inventory; the slot in both GUIs reverts to its glass-pane placeholder; both sides' confirmation resets; an item-pickup sound plays | pixel | TradeGUI |
| ultitrade.session.item-remove.neg-full-inventory | Same as `ultitrade.session.item-remove` above, except the acting player's regular inventory must be completely full (36/36 non-empty slots) at the moment of the removal AND no inventory slot may hold a stack of the staked item's own material below its maximum stack size — otherwise the removal merges into that stack, one slot does change and less than the full amount reaches the ground, and this row would score a correct run as a failure. Stake a single unstackable item the acting player is carrying nowhere else (a named or uniquely enchanted tool is simplest). Before the trade window is opened, all 36 of the acting player's own slots are non-empty, one of them holding that item. The acting player notes the exact material and stack size placed in the slot | (1) Stake the item exactly as `ultitrade.session.item-place` above describes. The slot it came from is now the acting player's only free slot: staking can never reduce the number of free slots, so a 36/36 inventory cannot be arranged before the stake and has to be restored after it — do not "simplify" this step away. (2) From the server console (not from the acting player, whose chat is unreachable while a container screen is open), run `give <acting player> minecraft:shield 1` and confirm server-side that the acting player is back to 36/36 non-empty slots; a shield is unstackable and is not the staked material, so it neither merges into the removal nor is merged into by it. (3) Click the staked slot again with an empty cursor | The item is NOT destroyed: exactly one item entity of that same material and stack size is lying on the ground at the acting player's feet, and it can be picked up after freeing one inventory slot; the acting player's own 36 inventory slots still hold what they held before the click (the item did not fit, so nothing there changed); the slot reverts to its glass-pane placeholder in both GUIs and the other player's side shows that slot empty; both sides' confirmation resets; the trade is still active (neither GUI closes) | pixel | TradeGUI |
| ultitrade.session.drag-blocked | An active `TradeGUI` session (`ultitrade.session.start` above); the acting player has lifted a stack of at least TWO items onto the cursor from one of their own inventory slots (the lift `ultitrade.session.item-place` above describes). Two is the minimum because vanilla admits a slot to a drag only while the carried stack holds more items than the slots already in it, so a one-item sweep is re-dispatched as an ordinary click and builds no drag event at all; there is also at least one pair of EMPTY slots in the acting player's own inventory | (1) Click-drag across trade-window slots 39 and 41 — the only two slots the window leaves genuinely empty (they appear in none of its slot arrays and no update writes them). Vanilla admits a slot to a drag only if it could actually place into it, so a drag across two "Your Items" slots holding placeholder panes is discarded before any event exists and would pass this row vacuously. (2) Click-drag across two empty slots of the acting player's own inventory | The window drag is refused entirely (event cancelled): slots 39 and 41 are both still empty, nothing is staked on either side, and the full carried stack is still on the cursor — only a single click-place (`ultitrade.session.item-place` above) stakes an item. The own-inventory drag is NOT refused: the carried stack is distributed across those two own slots exactly as it would be with no plugin present, because the acting player's own inventory is not part of anybody's offer (`UltiKits/UltiTrade#39`) | pixel | TradeGUI |
| ultitrade.session.chat-input | An active `TradeGUI` session; `enable-money-trade: true` and Vault installed (for the money variant) or `enable-exp-trade: true` (for the experience variant, shipped default, needs no Vault) | Click the acting player's own money (or experience) display slot | The GUI closes; chat reads (gloss) "Please enter the amount of money to trade in chat:" (gold) then (gloss) "(Type 'cancel' to cancel)" (gray) — or the experience-flavored equivalent, additionally showing the player's own total experience | server | |
| ultitrade.session.chat-input.neg-cancel-keyword | A money/experience input prompt is pending (`ultitrade.session.chat-input` above) | Send the chat message `cancel` | Chat reads (gloss) "Input cancelled" (yellow); the chat message itself is never broadcast to other players (event cancelled); the `TradeGUI` reopens unchanged | server | |
| ultitrade.session.chat-input.neg-insufficient-balance | A money input prompt is pending (`ultitrade.session.chat-input` above, money variant); Vault installed; the acting player's balance is LESS than the amount about to be entered | Send a chat message naming an amount greater than the player's current Vault balance | Chat reads (gloss) "Insufficient balance!" (red); the `TradeGUI` reopens with the money offer UNCHANGED from before the prompt | server | |
| ultitrade.session.chat-input.neg-invalid-number | A money/experience input prompt is pending (`ultitrade.session.chat-input` above) | Send a chat message that is not a valid number (e.g. `abc`) | Chat reads (gloss) "Invalid amount!" (red); the `TradeGUI` reopens with the offer unchanged | server | |
| ultitrade.session.chat-input.neg-negative-number | A money/experience input prompt is pending (`ultitrade.session.chat-input` above) | Send a chat message naming a negative number (e.g. `-5`) | Chat reads (gloss) "Amount cannot be negative!" (red); the `TradeGUI` reopens with the offer unchanged | server | |
| ultitrade.session.confirm-toggle | An active `TradeGUI` session with at least a token offer on one side (any item, money, or experience — non-empty is not actually required, but keeps the row meaningful) | Click the acting player's own Confirm button (green "Confirm Trade") | The acting player's status indicator flips to lime wool "Confirmed" (green text) in both GUIs; the button becomes yellow "Click to cancel confirmation"; the OTHER player receives (gloss) "{name} has confirmed the trade!" (green) plus a sound; if the OTHER side was already confirmed too, the trade completes immediately (see `ultitrade.persistence.trade-log` below) | pixel | TradeGUI |
| ultitrade.session.confirm-toggle.neg-uncomfirm | The acting player has already confirmed (`ultitrade.session.confirm-toggle` above), the OTHER side has NOT yet confirmed | Click the Confirm button again (now reading "Click to cancel confirmation") | The acting player's status reverts to red wool "Not confirmed"; the trade does NOT complete | pixel | TradeGUI |
| ultitrade.session.confirm-large | Same as `ultitrade.session.confirm-toggle` above, except the total money OR total experience offered by the confirming side is at least `confirm-threshold` (10000, shipped default) | Click the Confirm button for the first time on this large offer | The trade is immediately auto-cancelled — both players' placed items return to their own inventories, both `TradeGUI` windows close, and a cancellation message is shown to both — because closing `TradeGUI` to open the confirmation page triggers the SAME auto-cancel handler as manually closing it (`TradeListener#onInventoryClose`), and that scheduled cancellation runs before the page's own open() call. `TradeConfirmPage` (a 5-row summary window) then still opens for the confirming player, showing the very items that were just returned (see `ultitrade.session.confirm-page` below). Known product defect, `UltiKits/UltiTrade#23` | pixel | TradeConfirmPage |
| ultitrade.session.confirm-page | `ultitrade.session.confirm-large` above has just opened this page — on a trade that `UltiKits/UltiTrade#23` has already auto-cancelled by this point; the offer being previewed has 4 OR MORE items on at least one side (see the note below for what fewer than 4 items shows) | Read the window | An info paper item (gold "⚠ Trade Confirmation") lists what the viewer gives (money/tax/experience/item-count) and receives (post-tax amounts) — these are the SAME items already returned to inventory by the auto-cancellation above, since cancellation never clears the session's own item map; for a side with 4 or more items, ONLY 2 real item-preview slots are shown, because `TradeConfirmPage#displayItems` writes its "N more items..." indicator to `startSlot + displaySlots - 1` — the SAME slot index as the 3rd preview item, overwriting it rather than using a genuine 4th slot; a side with exactly 3 items shows all 3 with no "more items" indicator. A lime "✔ Confirm Trade" button and a red "✖ Cancel" button are always present. Known product defects, `UltiKits/UltiTrade#22` and `#23` | pixel | TradeConfirmPage |
| ultitrade.session.confirm-page.neg-cancel-returns | `ultitrade.session.confirm-page` above is open | Click the "✖ Cancel" button | `TradeGUI` does NOT reopen — the callback's own `isTrading` check fails, because the underlying trade was already auto-cancelled before this page opened (`UltiKits/UltiTrade#23`); the viewer is left looking at whatever inventory (their own regular one) was open before, not the trade GUI. This is the opposite of the intended "cancel this step, return to the trade" behaviour | pixel | |
| ultitrade.session.confirm-page.neg-confirm-marks | `ultitrade.session.confirm-page` above is open | Click the "✔ Confirm Trade" button | `session.setConfirmed(...)` is set on the now-orphaned session object (no effect on any live trade, which was already cancelled per `UltiKits/UltiTrade#23`); the OTHER player still receives the "{name} has confirmed the trade!" chat message and sound, which is now misleading since the trade is already dead; `TradeGUI` does NOT reopen for the viewer, for the same `isTrading`-false reason as `.neg-cancel-returns` above. Separately, and independent of `#23`: reading `TradeService#confirmTrade`'s large-trade branch directly confirms it never checks `isBothConfirmed()` or calls `completeTrade` even when both sides mark confirmed — a genuine code defect that would still block completion even if `#23` were fixed. Known product defects, `UltiKits/UltiTrade#21` and `#23` | pixel | |
| ultitrade.session.close-cancels | An active `TradeGUI` session (`ultitrade.session.start` above); no money/experience chat input currently pending for the closing player | Close the `TradeGUI` inventory (Escape, or opening another inventory) | One tick later, the trade is cancelled for both sides exactly as in `ultitrade.session.cancel`'s item-return behaviour below; the closing player did not need to run `/trade cancel` explicitly | server | |
| ultitrade.session.close-cancels.neg-input-pending | A money/experience chat input prompt is pending (`ultitrade.session.chat-input` above) — this itself closes the GUI as part of prompting | (no additional close needed — the GUI is already closed by the prompt itself) | The trade is NOT cancelled by this particular close — `waitingForInput` being non-empty for this player suppresses the auto-cancel; the trade resumes normally once the chat input is answered (`ultitrade.session.chat-input`'s own follow-up reopens the GUI) | server | |
| ultitrade.session.cancel | An active `TradeGUI` session (`ultitrade.session.start` above) with at least one item placed by EACH side (`ultitrade.session.item-place` above, run once per player) | As the cancelling player, first click your own money or experience display slot: that prompt is the only way to reach the chat while the trade is still live, because it closes the window WITHOUT cancelling (`ultitrade.session.chat-input` and `ultitrade.session.close-cancels.neg-input-pending` above), whereas closing the window any other way cancels the trade one tick later and a container screen swallows every key a chat line would need. Then run `/trade cancel` — a command, not a chat message, so the pending prompt is left intact and does not consume it | Chat to both players reads (gloss) "Trade cancelled! (X cancelled the trade)" (red, with the cancelling player's name in the parenthetical reason); BOTH players' own previously-placed items are returned to their OWN inventory (not swapped) exactly as placed; the OTHER player's `TradeGUI` window closes, and the cancelling player's is already closed by the prompt in step 1 (nothing reopens it, since the reopen task finds the trade gone); a failure sound/particle plays to both | server | |
| ultitrade.session.cancel.neg-full-inventory | Same as `ultitrade.session.cancel` above, except ONE player's regular inventory must be completely full (36/36 non-empty slots) at the moment of the cancel, and no slot of theirs may hold a stack of their own staked material below its maximum stack size (same reason as `ultitrade.session.item-remove.neg-full-inventory` above: a merge would leave less than the full amount on the ground) | (1) Stake one item per player as `ultitrade.session.cancel` above requires. The slot that player's stake came from is now their only free slot — staking cannot reduce the number of free slots, so the 36/36 state has to be restored after the stake, not before it. (2) From the server console, run `give <that player> minecraft:shield 1` and confirm server-side that they are back to 36/36 non-empty slots. (3) Cancel as `ultitrade.session.cancel` above describes (money/experience prompt, then `/trade cancel`) | That player's returned item(s) do NOT vanish and do NOT block the cancellation — `Bukkit.Inventory#addItem`'s overflow map is dropped naturally at that player's own feet (`Player#getWorld#dropItemNaturally`) rather than being discarded; both players still receive the cancellation message, and the window of the player who did not use the money/experience prompt closes while the prompting player's is already closed by it, exactly as `ultitrade.session.cancel` above states | server | |
| ultitrade.session.quit-cancels | An active `TradeGUI` session (`ultitrade.session.start` above) with at least one item placed by the quitting player | The player about to quit disconnects from the server (not a kick) | The trade is cancelled exactly as in `ultitrade.session.cancel` above — the quitting player's placed item is returned to their own inventory (observable on next login) and the REMAINING player's GUI closes with the cancellation message and their own items returned; `waitingForInput` for the quitting player is cleared | server | |

## Placeholders

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.placeholder.blocked-count | PlaceholderAPI installed; the target has blocked exactly 2 other players (`ultitrade.settings.block` above, run twice against two different targets) | From another plugin or a PAPI-aware command (e.g. `/papi parse <target> %ultitrade_blocked_count%`), request `%ultitrade_blocked_count%` for the target | Returns the literal string `2` | server | |
| ultitrade.placeholder.is-trading | PlaceholderAPI installed; the target is currently inside an active trade (`ultitrade.session.start` above) | Request `%ultitrade_is_trading%` and, separately, `%ultitrade_in_trade%` for the target | Both return the literal string `true`, identically | server | |
| ultitrade.placeholder.last-trade-ago | PlaceholderAPI installed; the target completed a trade between 1 and 59 minutes ago (complete an end-to-end trade with a second player as in `ultitrade.session.start` above through both sides confirming, with the target giving something in it, then wait at least 1 minute) | Request `%ultitrade_last_trade_ago%` for the target | Returns a Chinese phrase meaning "{N} minutes ago" (the module's own `formatTimeAgo`, a number followed by a Chinese unit word and "ago") | server | |
| ultitrade.placeholder.last-trade-ago.neg-never-traded | PlaceholderAPI installed; the target has never completed a trade (a genuinely fresh player, `enable-trade-log: true` or not — this reads `PlayerTradeSettings`, not the log table) | Request `%ultitrade_last_trade_ago%` for the target | Returns the literal Chinese text meaning "never" (gloss), not an empty string, a placeholder token, or an error | server | |
| ultitrade.placeholder.last-trade-time | PlaceholderAPI installed; the target completed a trade at a known wall-clock time (complete an end-to-end trade as in `ultitrade.session.start` above through both sides confirming, then wait a few seconds for `TradeLogService#logCompletedTrade`'s asynchronous `updatePlayerStats` task to persist — reading the placeholder immediately after completion can race this async write) | Request `%ultitrade_last_trade_time%` for the target | Returns that time formatted `yyyy-MM-dd HH:mm`, matching the actual completion time within a minute | server | |
| ultitrade.placeholder.total-exp | PlaceholderAPI installed; the target has completed exactly one trade in which they GAVE 50 experience (complete an end-to-end trade as in `ultitrade.session.start` above, with the target offering 50 experience via `ultitrade.session.chat-input` above, through both sides confirming, then wait a few seconds for `TradeLogService#logCompletedTrade`'s asynchronous `updatePlayerStats` task to persist — reading the placeholder immediately after completion can race this async write) | Request `%ultitrade_total_exp%` and `%ultitrade_total_exp_traded%` for the target | Both return the literal string `50`, identically | server | |
| ultitrade.placeholder.total-money | PlaceholderAPI installed, Vault installed; the target has completed exactly one trade in which they GAVE 100.0 currency (complete an end-to-end trade as in `ultitrade.session.start` above, with the target offering 100.0 currency via `ultitrade.session.chat-input` above, through both sides confirming, then wait a few seconds for `TradeLogService#logCompletedTrade`'s asynchronous `updatePlayerStats` task to persist — reading the placeholder immediately after completion can race this async write) | Request `%ultitrade_total_money%` and `%ultitrade_total_money_traded%` for the target | Both return `100.00`, identically (2 decimal places) | server | |
| ultitrade.placeholder.total-trades | PlaceholderAPI installed; the target has completed exactly 2 trades (repeat the end-to-end trade sequence in `ultitrade.session.start` above twice, through both sides confirming each time, then wait a few seconds after the second completion for `TradeLogService#logCompletedTrade`'s asynchronous `updatePlayerStats` task to persist — reading the placeholder immediately after completion can race this async write) | Request `%ultitrade_total_trades%` for the target | Returns the literal string `2` | server | |
| ultitrade.placeholder.trade-enabled | PlaceholderAPI installed; the target has toggled trading OFF (`ultitrade.settings.toggle` above) | Request `%ultitrade_trade_enabled%` and `%ultitrade_enabled%` for the target | Both return the literal string `false`, identically | server | |
| ultitrade.placeholder.trade-enabled-display | Same as `ultitrade.placeholder.trade-enabled` above (target toggled off) | Request `%ultitrade_trade_enabled_display%` and `%ultitrade_enabled_display%` for the target | Both return the same two-character Chinese word meaning "off", identically, REGARDLESS of the server's `language` setting (`UltiKits/UltiTrade#16`) | server | |

## Lifecycle Hooks

The rows below exercise `UltiKits/UltiTrade#15`'s lifecycle-hook migration: `unregisterSelf()` is a
`final` framework template method as of UltiTools 6.3.0, and this module's own unload work moved
verbatim into the `onUnregister()` hook it invokes (see `FEATURES.md`'s `## Lifecycle Hooks`).
`/upm uninstall <name>` matches `<name>` exactly against each loaded module's runtime name, which is
the `name:` key of the module's own `plugin.yml` — `UltiTrade` for this module, identical to the
repository name — calls that module's `unregisterSelf()` on the running server, and then **deletes
the module's jar** from `plugins/UltiTools/plugins/` (the file whose `plugin.yml` `name:` matches),
even though its own chat reply tells the operator to delete the local file manually
(`PluginInstallUtils#uninstallPlugin` in the framework repository). The row's cleanup therefore
restores the jar from a copy taken beforehand.

`/ul reload <name>` calls that module's `reloadSelf()`, which as of 6.3.0 re-reads the module's
config files into the running beans before logging the framework's own line `Module '<name>'
reloaded.`, and then calls the module's `onReload()` hook, which for this module re-runs the Vault
economy lookup and reschedules the old-log cleanup task when `enable-trade-log` or
`cleanup-interval-hours` changed (`UltiKits/UltiTrade#26`); this module's
config file on a server is `plugins/UltiTools/pluginConfig/UltiTrade/config/trade.yml`. The two
framework lines quoted in `ultitrade.lifecycle.reload` and `ultitrade.lifecycle.reload-money-trade`
are localised by the framework itself, unlike this module's own strings, so those two rows alone carry
a `language: en` precondition.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.lifecycle.reload | `language: en` in `plugins/UltiTools/config.yml` (governs only the two framework lines quoted in Expected); `UltiTrade` loaded; `max-distance: 50` (shipped default) in `plugins/UltiTools/pluginConfig/UltiTrade/config/trade.yml`; a sender and a SECOND online player (the target) both hold `ultitrade.use`, both have trading enabled, neither is on the other's blocklist, neither is trading, no request is pending between them in either direction, and both stand in the same world between 11 and 50 blocks apart (for example 20) and do not move until step 4 is done; Vault installed together with an economy plugin that registers a Vault economy provider (`vault-info` from the server console names a provider, not `None`) and `enable-money-trade: true` (shipped default) in that `trade.yml` — with money trading on and Vault or its provider missing, every reload also logs the module's money-trading-disabled warning, which Expected below does not allow for, so if `vault-info` reads `None` or `enable-money-trade` is not `true`, record the row `blocked`, never `pass` or `fail`; `/ul` runs from the server console (it requires op) | 1. As the sender, run `/trade <target>`. 2. As the target, run `/trade deny`, so no request stays pending. 3. Edit `max-distance` to `10` in that `trade.yml` and save the file; from the server console, on the SAME running server (no restart), run `ul reload UltiTrade`. 4. As the sender, run `/trade <target>` again. 5. Cleanup: set `max-distance` back to `50`, save, and run `ul reload UltiTrade` from the console. | Step 1: the sender receives (gloss) "Trade request sent to {target}!" (green) — this proves the players are within the original 50-block limit; if it is refused as too far instead, the players are placed wrongly: record the row `blocked`, never `pass`. Step 3: the console shows `Module 'UltiTrade' reloaded.` and the console command reply `Module UltiTrade has been reloaded`, with no reload line from the module itself. Step 4: the sender receives (gloss) "Too far away to trade!" (red, the Chinese literal in `TradeService#sendRequest`), and no request notification or BossBar reaches the target — the edited `max-distance` took effect without a restart. On the pre-migration module the configuration was never re-read on `/ul reload`, so step 4 would send the request instead | server | |
| ultitrade.lifecycle.reload-money-trade | `language: en` in `plugins/UltiTools/config.yml` (governs only the two framework lines quoted in Expected); Vault installed together with an economy plugin that registers a Vault economy provider; `UltiTrade` loaded; the tester can stop and start the server; a sender and a SECOND player (the target) who both hold `ultitrade.use`, both have trading enabled, neither is on the other's blocklist, and who will stand in the same world within 50 blocks of each other (`max-distance: 50`, shipped default) once connected; `/ul` and `vault-info` run from the server console (both require op) | 1. Stop the server completely. While it is stopped, set `enable-money-trade: false` in `plugins/UltiTools/pluginConfig/UltiTrade/config/trade.yml` and save the file (an edit made while the server runs is overwritten from memory when it shuts down). Start the server; once it has started, open that `trade.yml` again and record the `enable-money-trade` value; then have both players connect and stand as described in Preconditions, neither trading and no request pending between them. 2. From the server console, run `vault-info` and record its `Economy:` line. 3. As the sender, run `/trade <target>`; as the target, run `/trade accept`, so both players see the trade GUI. 4. As the sender, record the item and display name in the sender's own money slot (inventory slot 36, the left-most slot of the fifth row); do not click it; press Escape to close the GUI and wait one second (closing the GUI cancels the trade). 5. Edit `enable-money-trade` to `true` in that `trade.yml` and save the file; from the server console, on the SAME running server (no restart), run `ul reload UltiTrade`. 6. Repeat step 3. 7. As the sender, record slot 36's item and display name again, then click slot 36. 8. As the sender, type `cancel` in chat, then run `/trade cancel` so no trade stays open (if the trade has already ended, that reply is not part of this row). 9. Edit `enable-money-trade` to `false`, save, and run `ul reload UltiTrade` from the console. 10. Repeat step 3; as the sender, record slot 36's item and display name, then press Escape and wait one second. 11. Cleanup: set `enable-money-trade` back to `true` (shipped default), save, and run `ul reload UltiTrade` from the console. | Step 1: the reopened `trade.yml` still reads `enable-money-trade: false`; if it reads `true`, the edit did not survive: record the row `blocked`, never `pass`. Step 2: the line reads `[Vault] Economy: <provider> [<plugins>]` naming a provider; if it reads `None` instead, stop and record the row `blocked`, never `pass`. Step 4: slot 36 holds a barrier named (gloss) "Money trading not enabled" (red, the Chinese literal in `TradeGUI#updateMoneyDisplay`) — this confirms money trading is off in the server process step 1 started with `enable-money-trade: false`, in which no economy lookup has run; if slot 36 holds a gold nugget instead, step 1 did not take effect: record the row `blocked`, never `pass`. Step 5: the console shows `Module 'UltiTrade' reloaded.` and the console command reply `Module UltiTrade has been reloaded`, with no reload line from the module itself. Step 7: slot 36 now holds a gold nugget named (gloss) "Your money: 0.00" with the lore line (gloss) "Click to change amount"; the click closes the GUI and the sender's chat reads (gloss) "Please enter the amount of money to trade in chat:" (gold) then (gloss) "(Type 'cancel' to cancel)" (gray) — money trading was turned on by the reload, without a restart. On the module before `UltiKits/UltiTrade#26`, step 7 would still show the barrier and the click would open no prompt. Step 10: slot 36 holds the barrier named (gloss) "Money trading not enabled" again — the reload turned money trading back off. The discriminating evidence is step 4's barrier turning into step 7's gold nugget and prompt within the one server process step 1 started with money trading off, with no restart between them | pixel | TradeGUI |
| ultitrade.lifecycle.unload | PlaceholderAPI installed and enabled; `UltiTrade` loaded on the running server from its jar `UltiTrade-<version>.jar` in `plugins/UltiTools/plugins/`, and that folder holds nothing but module jars that each contain a `plugin.yml` (any other file or subdirectory there makes `/upm uninstall` stop with a red "delete failed, file access error" reply before it reaches the UltiTrade jar), with PlaceholderAPI already enabled when `UltiTrade` loaded (otherwise the expansion is never registered — the module logs, in Chinese, that PlaceholderAPI was not found); the tester is an online player who is a server operator (`/upm` requires op; `/papi parse me` requires a player sender and PlaceholderAPI's `placeholderapi.parse` permission, which op carries); BEFORE step 1, copy `plugins/UltiTools/plugins/UltiTrade-<version>.jar` to a location outside the server directory, because step 3 deletes it | 1. As the tester, run `/papi parse me %ultitrade_total_trades%` and record the reply. 2. Confirm that reply is a non-negative integer (for example `0` for a player who has never traded). If it is instead the literal text `%ultitrade_total_trades%`, the expansion was never registered: stop and record this row `blocked`, never `pass`. 3. On the SAME running server (no restart, no stop), run `/upm uninstall UltiTrade`. 4. Run `/papi parse me %ultitrade_total_trades%` again. 5. Cleanup: copy the saved jar back into `plugins/UltiTools/plugins/` and restart the server (not `/ul reload`, which does not load jars), then repeat step 1 and confirm it again returns a non-negative integer. | Step 3: the reply is the framework's green "uninstalled" message (not its red "uninstall failed" message, which would mean the runtime name did not match); the console shows the module's own "UltiTrade disabled!" line (English gloss of the Chinese literal in `UltiTrade#onUnregister`, printed identically under either `language`, `UltiKits/UltiTrade#16`); `plugins/UltiTools/plugins/` no longer contains `UltiTrade-<version>.jar`. Step 4: the reply is exactly the literal text `%ultitrade_total_trades%`, unchanged — PlaceholderAPI no longer resolves the `ultitrade` identifier because `onUnregister()` unregistered the expansion. The discriminating evidence is step 2's integer turning into step 4's literal on one server process, with no restart between them. Step 5: the integer reply is back, proving the restore left the module working | server | |

## Data persistence

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.persistence.trade-log | Two online players complete a trade end-to-end (`ultitrade.session.start` through both confirming); `enable-trade-log: true` (shipped default) | Read the `trade_logs` table directly (module's own SQLite database, `plugins/UltiTools/sqliteDB/UltiTrade.db (per SQLiteDataStore's dbPathForPlugin: a module's default getDataOperator call resolves to <UltiTools core data folder>/sqliteDB/<pluginName>.db, not a per-plugin subfolder)`) for the row matching both player UUIDs and the approximate completion time | A row exists with `status = 'COMPLETED'`, both players' serialized item JSON, money/experience amounts, and tax collected matching what was actually traded | server | |
| ultitrade.persistence.trade-log.neg-cancelled | Two online players start a trade, place at least one item each, then one runs `/trade cancel` (`ultitrade.session.cancel` above) | Read the `trade_logs` table for the row matching this session | A row exists with `status = 'CANCELLED'` and a non-null `cancel_reason` naming the cancelling player, distinct from a completed row's `status` | server | |
| ultitrade.persistence.settings-survive-restart | A player has toggled trading off and blocked one other player (`ultitrade.settings.toggle` and `ultitrade.settings.block` above) | Restart the server completely (not `/ul reload`), then re-check the player's state via `%ultitrade_trade_enabled%` (PlaceholderAPI) or by having them attempt to send a request to the blocked player | The trade-toggle state is still `false` and the blocklist still contains the blocked player after the restart — `TradeLogService#saveSettings` persisted both to `trade_player_settings` asynchronously before the change, and `getSettings`/`getOrCreateSettings` re-query it by `player_uuid` on first access after restart | server | |
| ultitrade.persistence.log-retention | `enable-trade-log: true`, `log-retention-days: 1` (NOT the shipped default of 30 — lowered for testability), `cleanup-interval-hours: 1` (NOT the shipped default of 24); a `trade_logs` row exists whose `trade_time` an admin has directly set, in the database, to more than 1 day in the past | Wait up to 1 hour (the lowered `cleanup-interval-hours`) with the server running | The artificially-aged `trade_logs` row is deleted — confirmed by re-reading the table directly and finding it absent, and (if any rows were deleted) a console log line reading "Cleaned up N expired trade logs (older than 1 days)" | server | |

## Configuration

One row per `@ConfigEntity` class (D-06's config-per-file rule), not per key: `TradeConfig`
(`config/trade.yml`, 24 keys), matching `FEATURES.md`'s `## Configuration` section exactly. This
row confirms every key is present at its documented default and that none of the seven keys this
version removed is written into a fresh file (`trade-timeout`, `UltiKits/UltiTrade#18`;
`messages.toggle-on`, `messages.toggle-off`, `messages.block-success`, `messages.unblock-success`,
`messages.already-blocked`, `messages.not-blocked`, `UltiKits/UltiTrade#17`), then flips one
representative interval-style key and observes the behaviour follow. Every key it lists is read.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.config.trade-yml | Fresh `config/trade.yml` at its shipped default | Load the file; confirm all 24 keys listed under `FEATURES.md`'s `## Configuration` section are present at their documented defaults, and that the file has none of the seven removed keys — no `trade-timeout` (`UltiKits/UltiTrade#18`), and under `messages:` exactly the seven keys `request-sent`, `request-received`, `request-timeout`, `trade-complete`, `trade-cancelled`, `trade-disabled`, `player-blocked` (`UltiKits/UltiTrade#17`); a file carried over from an earlier version does not count as fresh; then STOP THE SERVER COMPLETELY (not `/ul reload` — this row verifies the value as loaded at server start; applying an edit through `/ul reload UltiTrade` is verified separately by `ultitrade.lifecycle.reload`), set `request-timeout: 5` (default 30) in the file while the server is stopped (an edit made while the server runs is overwritten from memory when it shuts down), start the server, reopen the file and confirm it still reads `request-timeout: 5` (if it reads `30`, record the row `blocked`), then repeat `ultitrade.request.timeout-cleanup` above, confirming the request expires within the next fixed 10-second cleanup tick after the 5 seconds elapse (NOT a flat "around 5 seconds" — `cleanupExpiredRequests` runs every fixed 200 ticks regardless of `request-timeout`'s value, so actual expiration can take up to roughly 15 seconds depending on timing). | All 24 keys present at their documented defaults, and none of the seven removed keys, before the change; after lowering `request-timeout` to 5, a sent request expires (BossBar disappears, timeout message shown) within the next 10-second cleanup tick after 5 seconds have elapsed — up to roughly 15 seconds total, not a flat 5 — proving the lowered value took effect | server | |
