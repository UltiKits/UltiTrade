# UltiTrade — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the
steps to exercise it and the observable truth that proves it works. It is an internal reference
for real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`. **Nearly every meaningful row in this repository needs a
> SECOND online player** as the trade partner — a trade is inherently two-sided. Every row below
> states this explicitly in its own Preconditions rather than leaving it implied, because the
> executing agent has to arrange the second account before the row can even start.

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only,
  as `.neg-<slug>` — a negative case still tests the same feature, so the base ID is unchanged.
- **Layer**, copied verbatim from Laojun's own `ultitools-real-client-uat` skill so no
  translation step exists at dispatch time: `protocol`, `java-client`, `os-input`, `pixel`,
  `server`, `human`.
- **Human-authenticated-session rows (D-27b):** a row whose Steps can only be exercised through
  the maintainer's own authenticated UltiCloud panel session carries the fixed Preconditions
  phrase `maintainer-authenticated UltiCloud panel session (personal credentials)` and Layer
  `human`. This repository has no such row — it has no panel-capability surface of its own — so
  none is currently affected; the convention is stated here for template consistency.
- **Expected** must name an observable truth — an exact chat line, a log line, a database row,
  an inventory slot — and never the words "it works".
- **Covers** back-references a Phase 9 GUI-excluded class name; left blank when no such class
  applies. `TradeGUI` and `TradeConfirmPage` are this module's two entries in that register
  (`.planning/phases/09-module-ecosystem-readiness-and-test-coverage/gui-exclusions/
  UltiTrade.md`), so every pixel-layer row exercising either GUI names it in this column.
- A row whose Preconditions name a prior CHECKLIST row must appear after that row in file order —
  asserted mechanically: for every row, every checklist ID cited in its Preconditions cell must
  have a strictly smaller line number in this file than the row citing it (sweep class 8, D-27a).
  **A dependency on a row from a DIFFERENT, later-appearing section is described in prose (the
  command to run), never by citing that row's ID** — this avoids a forward reference while still
  telling the executor exactly how to reach the state.
- **Config-per-file rule (D-06):** one checklist row per `@ConfigEntity`-annotated class, never
  one row per key. This module ships exactly one `@ConfigEntity` (`TradeConfig`,
  `config/trade.yml`), so exactly one config row exists below (ID suffixed `-yml`,
  `ultitrade.config.trade-yml`). This module ships no Maven-filtered (build-time) config file, so
  the build-time-property clause of this rule does not apply to any row below.
- **This module's actual language behaviour (read `FEATURES.md`'s own Conventions note in full
  before any row below):** `language: en` has zero observable effect anywhere in this module —
  every player-facing string is a hardcoded Chinese literal or a Chinese-only config-default
  message. No row below carries a `language: en` precondition; every Expected quotation is given
  as an English gloss of the actual (Chinese) text the handler emits, with the literal characters
  left to the source line cited in `FEATURES.md`, per this document's English-only rule. Filed as
  `UltiKits/UltiTrade#16`.
- **Item-safety is this module's own hazard class (T-10-21):** every negative row that could lose
  or duplicate an item states the EXACT item state on both sides afterward, not merely "the trade
  is cancelled".

## Trade Requests

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.request.send | Sender and a SECOND online player (the target) both hold `ultitrade.use`; neither has trading toggled off (shipped default: enabled); neither is on the other's blocklist; neither is already trading; `max-distance: 50` (shipped default) and both players within that distance in the same world; no pending request already exists between them in either direction | Run `/trade <target>` | Chat line to the sender reads (English gloss of the actual Chinese) "Trade request sent to {target}!" (green); the target receives EITHER a clickable chat message reading "{sender} wants to trade with you! [Accept] [Deny]" (if `enable-clickable-buttons: true`, shipped default) OR, if that key is false, a plain line reading "{sender} wants to trade with you! Use /trade accept to accept"; a countdown BossBar titled "{sender} wants to trade with you (30s remaining)" appears to the target (since `enable-bossbar: true`, shipped default), counting down from `request-timeout` (30, shipped default) | server | |
| ultitrade.request.send.neg-already-pending | Same as `ultitrade.request.send` above, with a request from the sender to the SAME target already pending (run `ultitrade.request.send` above first, without the target responding) | Run `/trade <target>` again, to the same target | Chat line reads (gloss) "You have already sent a trade request to this player!" (red) — no second BossBar or notification reaches the target | server | |
| ultitrade.request.send.neg-blocked-by-target | Sender and target both online, both trade-enabled; the TARGET has blocked the sender (as the target, run `/trade block <sender>` first) | As the sender, run `/trade <target>` | Chat line to the sender reads (gloss) "The other party has blocked you!" (translated from `messages.player-blocked`'s shipped Chinese default); no BossBar or notification reaches the target | server | |
| ultitrade.request.send.neg-self | Sender online, trade-enabled | Run `/trade <sender's own name>` | Chat line reads (gloss) "You cannot trade with yourself!" (red); no request is created | server | |
| ultitrade.request.send.neg-target-disabled | Sender online, trade-enabled; the TARGET has toggled trading off (as the target, run `/trade toggle` first, confirming the disabled state via the target's own `/trade` bare-command status line) | As the sender, run `/trade <target>` | Chat line to the sender reads (gloss) "The other party has disabled trading!" (translated from `messages.trade-disabled`'s shipped Chinese default); no BossBar or notification reaches the target | server | |
| ultitrade.request.send.neg-target-offline | Sender online, `<name>` names a player who is NOT currently connected (may have played before, or never) | Run `/trade <name>` | Chat line reads (gloss) "Player {name} is not online!" (red); no request is created | server | |
| ultitrade.request.send.neg-target-trading | Sender online, trade-enabled, NOT already trading; the target is already in an active trade session with a THIRD online player — arrange this directly: have the third player run `/trade <target>`, then have the target run `/trade accept`, confirming both players now see an open `TradeGUI` before proceeding | As the sender, run `/trade <target>` (the busy one) | Chat line reads (gloss) "{target} is currently trading!" (red); no request reaches the busy target | server | |
| ultitrade.request.shift-click | Same preconditions as `ultitrade.request.send` above; `enable-shift-click: true` (shipped default) | Sneak (shift) and right-click the target player's entity model with an empty or item-holding main hand | Identical outcome to `ultitrade.request.send` (same chat line, BossBar, and target notification) — this is a second entry point to the same `TradeService#sendRequest` method | os-input | |
| ultitrade.request.shift-click.neg-disabled | Same as `ultitrade.request.shift-click` above, except `enable-shift-click: false` | Sneak and right-click the target player's entity model | Nothing happens from this module's perspective — no request, no message, no BossBar; the interact event passes through unmodified (`event.setCancelled(true)` is never reached) | os-input | |
| ultitrade.request.accept | The RECEIVING player has a pending trade request from a SECOND, still-online player (send it via `ultitrade.request.send` above) | As the receiver, run `/trade accept` | Both players' `TradeGUI` windows open simultaneously (see `ultitrade.session.start` below); the receiving player's own BossBar (if shown) disappears | pixel | TradeGUI |
| ultitrade.request.accept.neg-expired | A trade request was sent (`ultitrade.request.send` above); the receiver waits at least 40 seconds (past the 30-second `request-timeout` shipped default, plus a safety margin covering the 10-second cleanup tick) WITHOUT accepting or denying, confirming the receiver's BossBar (if shown) has already disappeared on its own | As the receiver, run `/trade accept` after that wait | Chat line reads (gloss) "No pending trade request!" (red) — the scheduled cleanup task already removed the expired entry; no GUI opens | server | |
| ultitrade.request.accept.neg-none-pending | The receiver has no pending trade request from anyone | Run `/trade accept` | Chat line reads (gloss) "No pending trade request!" (red) | server | |
| ultitrade.request.accept.neg-sender-offline | A trade request is pending (`ultitrade.request.send` above), then the ORIGINAL SENDER disconnects before the receiver accepts | As the receiver, run `/trade accept` | Chat line reads (gloss) "The other party is offline!" (red); no GUI opens; the pending-request entry is consumed (a second `/trade accept` immediately after reads "No pending trade request!" instead) | server | |
| ultitrade.request.deny | The RECEIVING player has a pending trade request from a SECOND, still-online player (send it via `ultitrade.request.send` above) | As the receiver, run `/trade deny` | Chat line to the receiver reads (gloss) "Trade request denied!" (yellow); the original sender receives (gloss) "{receiver} denied your trading request!" (red) plus a refusal sound; the receiver's BossBar (if shown) disappears | server | |
| ultitrade.request.deny.neg-none-pending | The receiver has no pending trade request from anyone | Run `/trade deny` | Chat line reads (gloss) "No pending trade request!" (red) | server | |
| ultitrade.request.timeout-cleanup | A trade request is pending (`ultitrade.request.send` above); the receiver does NOT accept or deny it | Wait `request-timeout` seconds (30, shipped default) plus a small margin, without either player taking action | Within the next 10-second cleanup tick after the timeout elapses, the receiver's BossBar (if shown) disappears and the receiver gets (gloss) "Trade request has timed out!" (red); a subsequent `/trade accept` from the receiver now reads "No pending trade request!" (see `.neg-none-pending` above) | server | |

## Trading Preferences

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.settings.block | Sender online; a SECOND online player (the target) not already on the sender's blocklist, and not the sender themself | Run `/trade block <target>` | Chat line reads (gloss) "{target} has been added to your trade blacklist!" (green), followed by (gloss) "This player will not be able to send you trade requests." (gray); confirmed by the target then attempting `/trade <sender>` and receiving `ultitrade.request.send.neg-blocked-by-target`'s refusal | server | |
| ultitrade.settings.block.neg-already-blocked | Sender online; target already on the sender's blocklist (run `ultitrade.settings.block` above first) | Run `/trade block <target>` again | Chat line reads (gloss) "{target} is already in your trade blacklist!" (red); the blocklist is unchanged | server | |
| ultitrade.settings.block.neg-offline | Sender online, `<name>` not currently connected | Run `/trade block <name>` | Chat line reads (gloss) "Player {name} is not online! Cannot add to blacklist." (red) | server | |
| ultitrade.settings.block.neg-self | Sender online | Run `/trade block <sender's own name>` | Chat line reads (gloss) "Cannot add yourself to the blacklist!" (red) | server | |
| ultitrade.settings.help | Sender online, holds `ultitrade.use` | Run `/trade` with no arguments (or `/trade help`) | Chat starts with (gloss) "=== UltiTrade Help ===" (gold), lists all 7 sub-commands (accept/deny/cancel/toggle/block/unblock/the bare request format) with a one-line description each, then ends with (gloss) "Trade status: " followed by "Enabled" (green) or "Disabled" (red) reflecting the sender's own current `TradeLogService#isTradeEnabled` state | server | |
| ultitrade.settings.toggle | Sender online, currently trade-ENABLED (the shipped default for a player never seen before) | Run `/trade toggle` | Chat line reads (gloss) "Trade feature disabled! Other players will not be able to send you trade requests." (yellow); confirmed by `ultitrade.settings.help` above (bare `/trade`) now showing "Disabled" | server | |
| ultitrade.settings.toggle.neg-reenable | Sender currently trade-DISABLED (run `ultitrade.settings.toggle` above first) | Run `/trade toggle` again | Chat line reads (gloss) "Trade feature enabled! Other players can now send you trade requests." (green); confirmed by `ultitrade.settings.help` above now showing "Enabled" again | server | |
| ultitrade.settings.unblock | Sender online; target currently on the sender's blocklist (run `ultitrade.settings.block` above first) | Run `/trade unblock <target>` | Chat line reads (gloss) "{target} has been removed from your trade blacklist!" (green); confirmed by the target then successfully sending a trade request (`ultitrade.request.send` above), no longer refused | server | |
| ultitrade.settings.unblock.neg-not-blocked | Sender online; `<name>` NOT currently on the sender's blocklist | Run `/trade unblock <name>` | Chat line reads (gloss) "{name} is not in your trade blacklist!" (red) | server | |
| ultitrade.settings.unblock.neg-offline | Sender online, `<name>` not currently connected | Run `/trade unblock <name>` | Chat line reads (gloss) "Player {name} is not online! Cannot remove from blacklist." (red) | server | |

## Trade Session

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.session.start | A trade request has been accepted or auto-started (run `ultitrade.request.accept` above) | (no additional step — already exercised by `ultitrade.request.accept`) | Both players see a 6-row `TradeGUI` inventory: left glass-pane placeholders (lime, "Your Items") in the 16 `YOUR_SLOTS`, right glass-pane placeholders (cyan, "Their Items") in the 16 `THEIR_SLOTS`, black separator panes, a green "Confirm Trade" button, a red "Cancel Trade" barrier button, red/lime wool "not confirmed" status indicators on both sides, and (since `enable-money-trade`/`enable-exp-trade` default true, with no Vault installed in a Vault-less test environment) a red "Money trading not enabled" barrier in place of the money display if Vault is genuinely absent | pixel | TradeGUI |
| ultitrade.session.item-place | Both players in an active `TradeGUI` (`ultitrade.session.start` above); the acting player holds an item stack in their cursor | Click one of the acting player's own 16 "Your Items" slots (a lime glass-pane placeholder) while holding an item | The item moves from the cursor into that slot in the acting player's OWN GUI (glass pane replaced by the real item); the item ALSO appears (with an added detail-lore block: enchantments/durability/flags/unbreakable/custom-model, where applicable) in the corresponding "Their Items" slot of the OTHER player's GUI; both sides' confirmation status resets to "not confirmed" if either was previously confirmed; a note-block-pling sound plays to the placing player | pixel | TradeGUI |
| ultitrade.session.item-remove | An item was placed by the acting player in one of their own slots (`ultitrade.session.item-place` above) | Click that same slot again (now showing the real item, not a glass pane) | The item returns to the acting player's own inventory (or drops if the inventory is full — see `.neg-full-inventory` below for the trade-completion analogue); the slot in both GUIs reverts to its glass-pane placeholder; both sides' confirmation resets; an item-pickup sound plays | pixel | TradeGUI |
| ultitrade.session.drag-blocked | An active `TradeGUI` session (`ultitrade.session.start` above) | Attempt to drag-place an item stack across two or more slots of the open `TradeGUI` (e.g. shift-drag or click-drag spanning multiple slots) | The drag is refused entirely (event cancelled) — no item moves into any slot this way; only a single click-place (`ultitrade.session.item-place`) works | pixel | TradeGUI |
| ultitrade.session.chat-input | An active `TradeGUI` session; `enable-money-trade: true` and Vault installed (for the money variant) or `enable-exp-trade: true` (for the experience variant, shipped default, needs no Vault) | Click the acting player's own money (or experience) display slot | The GUI closes; chat reads (gloss) "Please enter the amount of money to trade in chat:" (gold) then (gloss) "(Type 'cancel' to cancel)" (gray) — or the experience-flavored equivalent, additionally showing the player's own total experience | server | |
| ultitrade.session.chat-input.neg-cancel-keyword | A money/experience input prompt is pending (`ultitrade.session.chat-input` above) | Send the chat message `cancel` | Chat reads (gloss) "Input cancelled" (yellow); the chat message itself is never broadcast to other players (event cancelled); the `TradeGUI` reopens unchanged | server | |
| ultitrade.session.chat-input.neg-insufficient-balance | A money input prompt is pending (`ultitrade.session.chat-input` above, money variant); Vault installed; the acting player's balance is LESS than the amount about to be entered | Send a chat message naming an amount greater than the player's current Vault balance | Chat reads (gloss) "Insufficient balance!" (red); the `TradeGUI` reopens with the money offer UNCHANGED from before the prompt | server | |
| ultitrade.session.chat-input.neg-invalid-number | A money/experience input prompt is pending (`ultitrade.session.chat-input` above) | Send a chat message that is not a valid number (e.g. `abc`) | Chat reads (gloss) "Invalid amount!" (red); the `TradeGUI` reopens with the offer unchanged | server | |
| ultitrade.session.chat-input.neg-negative-number | A money/experience input prompt is pending (`ultitrade.session.chat-input` above) | Send a chat message naming a negative number (e.g. `-5`) | Chat reads (gloss) "Amount cannot be negative!" (red); the `TradeGUI` reopens with the offer unchanged | server | |
| ultitrade.session.confirm-toggle | An active `TradeGUI` session with at least a token offer on one side (any item, money, or experience — non-empty is not actually required, but keeps the row meaningful) | Click the acting player's own Confirm button (green "Confirm Trade") | The acting player's status indicator flips to lime wool "Confirmed" (green text) in both GUIs; the button becomes yellow "Click to cancel confirmation"; the OTHER player receives (gloss) "{name} has confirmed the trade!" (green) plus a sound; if the OTHER side was already confirmed too, the trade completes immediately (see `ultitrade.persistence.trade-log` below) | pixel | TradeGUI |
| ultitrade.session.confirm-toggle.neg-uncomfirm | The acting player has already confirmed (`ultitrade.session.confirm-toggle` above), the OTHER side has NOT yet confirmed | Click the Confirm button again (now reading "Click to cancel confirmation") | The acting player's status reverts to red wool "Not confirmed"; the trade does NOT complete | pixel | TradeGUI |
| ultitrade.session.confirm-large | Same as `ultitrade.session.confirm-toggle` above, except the total money OR total experience offered by the confirming side is at least `confirm-threshold` (10000, shipped default) | Click the Confirm button for the first time on this large offer | `TradeConfirmPage` opens (a 5-row summary window) instead of confirming immediately (see `ultitrade.session.confirm-page` below) | pixel | TradeConfirmPage |
| ultitrade.session.confirm-page | `ultitrade.session.confirm-large` above has just opened this page | Read the window | An info paper item (gold "⚠ Trade Confirmation") lists what the viewer gives (money/tax/experience/item-count) and receives (post-tax amounts); up to 3 item-preview slots per side (a 4th slot shows "N more items..." if more than 3); a lime "✔ Confirm Trade" button and a red "✖ Cancel" button | pixel | TradeConfirmPage |
| ultitrade.session.confirm-page.neg-cancel-returns | `ultitrade.session.confirm-page` above is open | Click the "✖ Cancel" button | The viewer returns to the ordinary `TradeGUI` UNCHANGED — the trade itself is NOT cancelled, only this confirmation step; the viewer's own confirmation status is still "not confirmed" | pixel | TradeGUI |
| ultitrade.session.confirm-page.neg-confirm-marks | `ultitrade.session.confirm-page` above is open | Click the "✔ Confirm Trade" button | The viewer's confirmation is marked exactly as in `ultitrade.session.confirm-toggle`'s positive case, and the viewer returns to `TradeGUI` showing their own side now confirmed | pixel | TradeGUI |
| ultitrade.session.close-cancels | An active `TradeGUI` session (`ultitrade.session.start` above); no money/experience chat input currently pending for the closing player | Close the `TradeGUI` inventory (Escape, or opening another inventory) | One tick later, the trade is cancelled for both sides exactly as in `ultitrade.session.cancel`'s item-return behaviour below; the closing player did not need to run `/trade cancel` explicitly | server | |
| ultitrade.session.close-cancels.neg-input-pending | A money/experience chat input prompt is pending (`ultitrade.session.chat-input` above) — this itself closes the GUI as part of prompting | (no additional close needed — the GUI is already closed by the prompt itself) | The trade is NOT cancelled by this particular close — `waitingForInput` being non-empty for this player suppresses the auto-cancel; the trade resumes normally once the chat input is answered (`ultitrade.session.chat-input`'s own follow-up reopens the GUI) | server | |
| ultitrade.session.cancel | An active `TradeGUI` session (`ultitrade.session.start` above) with at least one item placed by EACH side (`ultitrade.session.item-place` above, run once per player) | As either player, run `/trade cancel` | Chat to both players reads (gloss) "Trade cancelled! (X cancelled the trade)" (red, with the cancelling player's name in the parenthetical reason); BOTH players' own previously-placed items are returned to their OWN inventory (not swapped) exactly as placed; both `TradeGUI` windows close; a failure sound/particle plays to both | server | |
| ultitrade.session.cancel.neg-full-inventory | Same as `ultitrade.session.cancel` above, except ONE player's regular inventory is completely full (36/36 non-empty slots) before the cancel | Run `/trade cancel` | That player's returned item(s) do NOT vanish and do NOT block the cancellation — `Bukkit.Inventory#addItem`'s overflow map is dropped naturally at that player's own feet (`Player#getWorld#dropItemNaturally`) rather than being discarded; both players still receive the cancellation message and both GUIs still close | server | |
| ultitrade.session.quit-cancels | An active `TradeGUI` session (`ultitrade.session.start` above) with at least one item placed by the quitting player | The player about to quit disconnects from the server (not a kick) | The trade is cancelled exactly as in `ultitrade.session.cancel` above — the quitting player's placed item is returned to their own inventory (observable on next login) and the REMAINING player's GUI closes with the cancellation message and their own items returned; `waitingForInput` for the quitting player is cleared | server | |

## Placeholders

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.placeholder.blocked-count | PlaceholderAPI installed; the target has blocked exactly 2 other players (`ultitrade.settings.block` above, run twice against two different targets) | From another plugin or a PAPI-aware command (e.g. `/papi parse <target> %ultitrade_blocked_count%`), request `%ultitrade_blocked_count%` for the target | Returns the literal string `2` | server | |
| ultitrade.placeholder.is-trading | PlaceholderAPI installed; the target is currently inside an active trade (`ultitrade.session.start` above) | Request `%ultitrade_is_trading%` and, separately, `%ultitrade_in_trade%` for the target | Both return the literal string `true`, identically | server | |
| ultitrade.placeholder.last-trade-ago | PlaceholderAPI installed; the target completed a trade between 1 and 59 minutes ago (complete an end-to-end trade with a second player as in `ultitrade.session.start` above through both sides confirming, with the target giving something in it, then wait at least 1 minute) | Request `%ultitrade_last_trade_ago%` for the target | Returns a Chinese phrase meaning "{N} minutes ago" (the module's own `formatTimeAgo`, a number followed by a Chinese unit word and "ago") | server | |
| ultitrade.placeholder.last-trade-ago.neg-never-traded | PlaceholderAPI installed; the target has never completed a trade (a genuinely fresh player, `enable-trade-log: true` or not — this reads `PlayerTradeSettings`, not the log table) | Request `%ultitrade_last_trade_ago%` for the target | Returns the literal Chinese text meaning "never" (gloss), not an empty string, a placeholder token, or an error | server | |
| ultitrade.placeholder.last-trade-time | PlaceholderAPI installed; the target completed a trade at a known wall-clock time (complete an end-to-end trade as in `ultitrade.session.start` above through both sides confirming) | Request `%ultitrade_last_trade_time%` for the target | Returns that time formatted `yyyy-MM-dd HH:mm`, matching the actual completion time within a minute | server | |
| ultitrade.placeholder.total-exp | PlaceholderAPI installed; the target has completed exactly one trade in which they GAVE 50 experience (complete an end-to-end trade as in `ultitrade.session.start` above, with the target offering 50 experience via `ultitrade.session.chat-input` above, through both sides confirming) | Request `%ultitrade_total_exp%` and `%ultitrade_total_exp_traded%` for the target | Both return the literal string `50`, identically | server | |
| ultitrade.placeholder.total-money | PlaceholderAPI installed, Vault installed; the target has completed exactly one trade in which they GAVE 100.0 currency (complete an end-to-end trade as in `ultitrade.session.start` above, with the target offering 100.0 currency via `ultitrade.session.chat-input` above, through both sides confirming) | Request `%ultitrade_total_money%` and `%ultitrade_total_money_traded%` for the target | Both return `100.00`, identically (2 decimal places) | server | |
| ultitrade.placeholder.total-trades | PlaceholderAPI installed; the target has completed exactly 2 trades (repeat the end-to-end trade sequence in `ultitrade.session.start` above twice, through both sides confirming each time) | Request `%ultitrade_total_trades%` for the target | Returns the literal string `2` | server | |
| ultitrade.placeholder.trade-enabled | PlaceholderAPI installed; the target has toggled trading OFF (`ultitrade.settings.toggle` above) | Request `%ultitrade_trade_enabled%` and `%ultitrade_enabled%` for the target | Both return the literal string `false`, identically | server | |
| ultitrade.placeholder.trade-enabled-display | Same as `ultitrade.placeholder.trade-enabled` above (target toggled off) | Request `%ultitrade_trade_enabled_display%` and `%ultitrade_enabled_display%` for the target | Both return the same two-character Chinese word meaning "off", identically, REGARDLESS of the server's `language` setting (`UltiKits/UltiTrade#16`) | server | |

## Data persistence

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.persistence.trade-log | Two online players complete a trade end-to-end (`ultitrade.session.start` through both confirming); `enable-trade-log: true` (shipped default) | Read the `trade_logs` table directly (module's own SQLite database, `plugins/UltiTools/sqliteDB/UltiTrade.db (per SQLiteDataStore's dbPathForPlugin: a module's default getDataOperator call resolves to <UltiTools core data folder>/sqliteDB/<pluginName>.db, not a per-plugin subfolder)`) for the row matching both player UUIDs and the approximate completion time | A row exists with `status = 'COMPLETED'`, both players' serialized item JSON, money/experience amounts, and tax collected matching what was actually traded | server | |
| ultitrade.persistence.trade-log.neg-cancelled | Two online players start a trade, place at least one item each, then one runs `/trade cancel` (`ultitrade.session.cancel` above) | Read the `trade_logs` table for the row matching this session | A row exists with `status = 'CANCELLED'` and a non-null `cancel_reason` naming the cancelling player, distinct from a completed row's `status` | server | |
| ultitrade.persistence.settings-survive-restart | A player has toggled trading off and blocked one other player (`ultitrade.settings.toggle` and `ultitrade.settings.block` above) | Restart the server completely (not `/ul reload`), then re-check the player's state via `%ultitrade_trade_enabled%` (PlaceholderAPI) or by having them attempt to send a request to the blocked player | The trade-toggle state is still `false` and the blocklist still contains the blocked player after the restart — `TradeLogService#saveSettings` persisted both to `trade_player_settings` asynchronously before the change, and `getSettings`/`getOrCreateSettings` re-query it by `player_uuid` on first access after restart | server | |
| ultitrade.persistence.log-retention | `enable-trade-log: true`, `log-retention-days: 1` (NOT the shipped default of 30 — lowered for testability), `cleanup-interval-hours: 1` (NOT the shipped default of 24); a `trade_logs` row exists whose `trade_time` an admin has directly set, in the database, to more than 1 day in the past | Wait up to 1 hour (the lowered `cleanup-interval-hours`) with the server running | The artificially-aged `trade_logs` row is deleted — confirmed by re-reading the table directly and finding it absent, and (if any rows were deleted) a console log line reading "Cleaned up N expired trade logs (older than 1 days)" | server | |

## Configuration

One row per `@ConfigEntity` class (D-06's config-per-file rule), not per key: `TradeConfig`
(`config/trade.yml`, 31 keys), matching `FEATURES.md`'s `## Configuration` section exactly. This
row confirms every key is present at its documented default, then flips one representative
interval-style key and observes the behaviour follow — **except the seven keys `FEATURES.md`
documents as having no observable effect** (`messages.toggle-on`, `messages.toggle-off`,
`messages.block-success`, `messages.unblock-success`, `messages.already-blocked`,
`messages.not-blocked`, `trade-timeout`), which this row deliberately does NOT attempt to
exercise for an effect, per the same "do not test a key with no effect" convention the
framework's own `UAT-CHECKLIST.md` applies to `ultipanel.logging.batch.interval`.

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultitrade.config.trade-yml | Fresh `config/trade.yml` at its shipped default | Load the file; confirm all 31 keys listed under `FEATURES.md`'s `## Configuration` section are present at their documented defaults; then set `request-timeout: 5` (default 30) and repeat `ultitrade.request.timeout-cleanup` above, confirming the request now expires around 5 seconds rather than 30. Do NOT vary `messages.toggle-on`, `messages.toggle-off`, `messages.block-success`, `messages.unblock-success`, `messages.already-blocked`, `messages.not-blocked`, or `trade-timeout` expecting an observable effect — none of the seven has one (see each key's own `FEATURES.md` row and filed issue) | All 31 keys present at their documented defaults before the change; after lowering `request-timeout` to 5, a sent request expires (BossBar disappears, timeout message shown) around 5 seconds later rather than 30, proving the lowered value took effect | server | |
